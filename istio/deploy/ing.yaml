apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gateway
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "istio"
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: appservice
          servicePort: 80